# Preprocess output

The preprocess script writes **`lead_1_duplicated/`** here: one 10 s .mat per record (Lead I duplicated to 12 channels, 500 Hz, standardized). No raw WFDB data is stored in this repo; point the script at your downloaded MIMIC-IV-ECG root with `--raw-root`.
